[build]
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "uvicorn api.main:app --host 0.0.0.0 --port ${PORT:-8000} --workers 1 --timeout-keep-alive 120"
healthcheckPath = "/health"
healthcheckTimeout = 60

[[mounts]]
mountPath = "/data"
